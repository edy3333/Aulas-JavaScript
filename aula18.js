// e se eu quiser mostrar cada valor do elemento especifico do array ?//
let vetor = [4,6,5,7,34,56]
vetor.sort()
for(let pos = 0; pos < vetor.length;pos++){
    console.log(`a posição tem valor ${pos} tem o valor ${vetor[pos]}`);
}